type data = {
    items: {}[];
}

export default function CardsMarket({items}:data){
    
    return(
        <>
            {/* {dataJson.map((element: {img: string, title: string, price: string}, key:any) =>{
                console.log(element)
                return(
                    <div key={key}>
                        <img src={element.img} alt={element.title} />
                    </div> 
                )
            })} */}
        </>
    )
}